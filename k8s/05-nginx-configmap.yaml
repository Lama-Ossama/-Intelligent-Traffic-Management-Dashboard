apiVersion: v1
kind: ConfigMap
metadata:
  name: nginx-config
  namespace: traffic-system
data:
  nginx.conf: |
    events {}
    http {
      server {
        listen 80;

        location / {
          proxy_pass http://traffic-dashboard:3002;
        }
      }
    }